/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['res.cloudinary.com'], // Remplace par le vrai domaine de tes images
    },
  };
  
  export default nextConfig;